#include <iostream>

using namespace std;

#include "includes/vectores.h"

int main()
{
    int vec[10];
    inicializadorVector(vec,10,0);
    mostrarVector(vec,10);
    system("pause>null");
    return 0;
}
